Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.3
Flask-JWT-Extended
PyPDF2==3.0.0
pdfplumber==0.7.5    # optional, recommended for robust PDF parsing
pillow       # optional (PIL)
pytesseract==0.3.10  # optional, requires tesseract binary on system
opencv-python-headless>=4.8.0  # image preprocessing for OCR (headless for server deploy)
joblib==1.4.2
pandas==2.1.1
# Use a numpy compatible with pandas on Python 3.11 and scikit-learn 1.7.2
numpy>=2.0.0
# Pin to 1.7.2 — exact version the .pkl models were serialized with
# (models include DummyClassifier, RandomForestClassifier, StandardScaler, LabelEncoder)
# Upgrading without re-training will cause InconsistentVersionWarning and potential errors
scikit-learn==1.7.2
seaborn==0.12.2
google-generativeai>=0.5.0   # Gemini API SDK
python-dotenv>=1.0.0         # load .env into os.environ
google-auth>=2.23.0
google-api-python-client>=2.100.0
pymongo[srv]>=4.9.0          # >=4.9.0 required for dnspython 2.7+/2.8+ compatibility
dnspython>=2.6.1,<3.0.0      # 2.6.1+ required by pymongo 4.9+
flask-socketio==5.3.6
eventlet>=0.35.2
reportlab>=4.0.0                  # PDF generation for health report export
requests>=2.31.0                   # API communication
sib-api-v3-sdk>=7.6.0              # Brevo API SDK
cloudinary>=1.36.0                 # Cloudinary SDK for certificate file uploads
google-auth-oauthlib>=1.2.0        # Google OAuth2 flow (used in google_auth route)
mediapipe>=0.10.0                  # MediaPipe for gesture/pose detection
xgboost>=2.0.0                     # XGBoost for heart disease prediction model
